# Инструкция по установке и запуску

Пошаговая инструкция для тех, кто только что склонировал репозиторий.

## ⚠️ Важно: Сначала прочитайте это!

Если что-то не работает, следуйте инструкциям **по порядку**, не пропуская шаги.

## Шаг 1: Проверка Python

Убедитесь, что Python установлен:

```bash
python --version
```

Должно быть Python 3.8 или выше. Если нет - установите с https://www.python.org/

## Шаг 2: Клонирование репозитория

```bash
git clone https://github.com/Egorka8982/kursovaya.git
cd kursovaya
```

## Шаг 3: Создание виртуального окружения

**Windows (PowerShell):**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

Если возникает ошибка выполнения скриптов:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**Windows (CMD):**
```cmd
python -m venv venv
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Проверка:** В начале строки должно появиться `(venv)`

## Шаг 4: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**Важно:** Дождитесь завершения установки всех пакетов. Это может занять несколько минут.

## Шаг 5: Проверка установки

Проверьте, что основные пакеты установлены:

```bash
python -c "import fastapi; import uvicorn; import selenium; print('Все пакеты установлены!')"
```

Если ошибка - повторите шаг 4.

## Шаг 6: Запуск приложения

```bash
python main.py
```

Должно появиться:
```
INFO:     Uvicorn running on http://0.0.0.0:8007
```

Откройте в браузере: http://localhost:8007

## Шаг 7: Запуск тестов (опционально)

**Только после того, как приложение запускается!**

1. Убедитесь, что Google Chrome установлен
2. Закройте приложение (Ctrl+C) если оно запущено
3. Запустите тесты:

```bash
pytest tests/ -v
```

## ❌ Решение проблем

### Ошибка: "ModuleNotFoundError"

**Решение:** Вы не активировали виртуальное окружение или не установили зависимости.
1. Активируйте venv (шаг 3)
2. Установите зависимости (шаг 4)

### Ошибка: "Port 8007 is already in use"

**Решение:** Порт занят другим процессом.
1. Закройте другое приложение на порту 8007
2. Или измените порт в `main.py` (строка с `port=8007`)

### Ошибка при установке зависимостей (требуется Rust)

**Решение:** Обновленные версии в requirements.txt должны работать. Если нет:
```bash
pip install --upgrade pip
pip install pydantic>=2.9.0 sqlalchemy>=2.0.36
```

### Тесты падают с ERROR

**Возможные причины:**
1. **Сервер не запускается** - проверьте что `python main.py` работает
2. **Chrome не установлен** - установите Google Chrome
3. **Нет интернета** - ChromeDriver скачивается автоматически, нужен интернет
4. **Зависимости не установлены** - выполните шаг 4

**Для отладки тестов:**
1. Откройте `tests/conftest.py`
2. Найдите строку: `chrome_options.add_argument("--headless")`
3. Закомментируйте её: `# chrome_options.add_argument("--headless")`
4. Запустите тесты снова - браузер откроется и вы увидите что происходит

### Ошибка: "FileNotFoundError: main.py"

**Решение:** Вы не в корневой папке проекта. Выполните:
```bash
cd kursovaya
```

## ✅ Проверочный список

Перед запуском убедитесь:
- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] Приложение запускается (`python main.py`)
- [ ] Браузер открывает http://localhost:8007

## 📞 Нужна помощь?

Если ничего не помогает:
1. Проверьте версию Python: `python --version`
2. Проверьте что venv активирован: должно быть `(venv)` в начале строки
3. Попробуйте переустановить зависимости: `pip install --force-reinstall -r requirements.txt`
4. Проверьте что вы в правильной папке: `ls main.py` (Linux/Mac) или `dir main.py` (Windows)

