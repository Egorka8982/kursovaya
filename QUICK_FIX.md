# Быстрое решение проблемы с тестами

## ✅ Диагностика показала что сервер работает

Если `python tests/debug_server.py` показывает успех, но тесты падают, выполните:

### 1. Обновите код из репозитория

```bash
git pull origin main
```

**Важно:** Убедитесь что у вас последняя версия всех файлов, особенно:
- `tests/conftest.py` 
- `tests/test_clients.py`
- `tests/test_main_page.py`
- `tests/test_animals.py`

### 2. Активируйте виртуальное окружение

**Windows PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows CMD:**
```cmd
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Проверьте что активировано:**
```bash
where python  # Windows
which python  # Linux/Mac
# Должен показать путь к venv\Scripts\python.exe или venv/bin/python
```

### 3. Установите/обновите зависимости

```bash
pip install -r requirements.txt
```

### 4. Запустите тесты с подробным выводом

```bash
pytest tests/ -v --tb=short
```

Если все еще падает, запустите один тест:

```bash
pytest tests/test_main_page.py::TestMainPage::test_open_main_page -v -s
```

Флаг `-s` покажет весь вывод (stdout/stderr).

### 5. Если тесты все еще падают

Проверьте что pytest использует правильный Python:

```bash
pytest --version
python --version
```

Они должны использовать Python из venv.

### 6. Альтернатива: запустите тесты через Python

```bash
python -m pytest tests/ -v
```

Это гарантирует использование правильного Python.

---

## 🔍 Если ничего не помогает

1. Удалите и пересоздайте venv:
```bash
# Windows
rmdir /s venv
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt

# Linux/Mac
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

2. Проверьте что порт свободен:
```bash
# Windows
netstat -ano | findstr :8007

# Linux/Mac
lsof -i :8007
```

3. Запустите сервер вручную в отдельном терминале:
```bash
python main.py
```

Затем в другом терминале запустите тесты:
```bash
pytest tests/ -v
```

---

## 📝 Частая проблема: "Сервер не доступен" в старых версиях кода

Если вы видите ошибку "Сервер не доступен. Убедитесь что сервер запущен" - это значит у вас **старая версия кода**.

**Решение:** Обновите код:
```bash
git pull origin main
```

В новой версии эти проверки убраны из тестов, так как фикстура `server_process` гарантирует запуск сервера.

