# Ветеринарная клиника - Система учета животных

Веб-приложение для управления ветеринарной клиникой с использованием FastAPI, SQLAlchemy и современной модульной архитектуры.

## 📁 Структура проекта

```
kursovaya/
├── app/                          # Основная папка приложения
│   ├── __init__.py
│   ├── core/                     # Ядро приложения
│   │   ├── __init__.py
│   │   ├── config.py             # Настройки приложения
│   │   └── database.py           # Подключение к БД
│   ├── models/                   # Модели базы данных (SQLAlchemy)
│   │   ├── __init__.py
│   │   ├── client.py             # Модель клиента
│   │   ├── animal.py             # Модель животного
│   │   ├── doctor.py             # Модель врача
│   │   ├── treatment.py          # Модель лечения
│   │   └── procedure.py          # Модель процедуры
│   ├── schemas/                   # Pydantic схемы (валидация данных)
│   │   ├── __init__.py
│   │   ├── client.py
│   │   ├── animal.py
│   │   ├── doctor.py
│   │   ├── treatment.py
│   │   └── procedure.py
│   └── routers/                  # API роутеры (эндпоинты)
│       ├── __init__.py
│       ├── clients.py            # Роуты для клиентов
│       ├── animals.py            # Роуты для животных
│       ├── doctors.py            # Роуты для врачей
│       ├── treatments.py         # Роуты для лечения
│       └── procedures.py        # Роуты для процедур
├── static/                       # Статические файлы (HTML, CSS, JS)
│   ├── common.css               # Общие стили
│   ├── common.js                # Общие JavaScript функции
│   ├── index.html               # Главная страница
│   ├── client.html              # Страница клиентов
│   ├── animal.html              # Страница животных
│   ├── animal_doctor.html       # Страница врачей
│   ├── treatment.html           # Страница лечения
│   └── procedure.html          # Страница процедур
├── main.py                      # Точка входа приложения
├── requirements.txt             # Зависимости проекта
├── .gitignore                   # Игнорируемые файлы Git
└── README.md                    # Этот файл
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/Egorka8982/kursovaya.git
cd kursovaya
```

### 2. Создание виртуального окружения

**Windows:**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

> **Примечание:** Если при активации в PowerShell возникает ошибка, выполните:
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск приложения

```bash
python main.py
```

Или через uvicorn напрямую:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8007
```

### 5. Открытие в браузере

Откройте: http://localhost:8007

## 📋 Зависимости

- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **Uvicorn** - ASGI сервер
- **Pydantic** - валидация данных
- **Pydantic-settings** - управление настройками

Полный список зависимостей в `requirements.txt`

## 🗄️ База данных

Проект использует **SQLite** (файл `animal_hospital.db`). База данных создается автоматически при первом запуске.

### Переключение на PostgreSQL

Если хотите использовать PostgreSQL вместо SQLite:

1. Установите драйвер:
```bash
pip install psycopg2-binary
```

2. Измените `DATABASE_URL` в `app/core/config.py`:
```python
DATABASE_URL: str = "postgresql://user:password@localhost/dbname"
```

Или создайте файл `.env`:
```
DATABASE_URL=postgresql://user:password@localhost/dbname
```

## 🏗️ Архитектура проекта

### Модульная структура

Проект организован по принципу **разделения ответственности**:

- **`core/`** - настройки и подключение к БД
- **`models/`** - модели данных (SQLAlchemy ORM)
- **`schemas/`** - схемы валидации (Pydantic)
- **`routers/`** - API эндпоинты (FastAPI роутеры)

### Как работает ORM

```python
# Пример: создание животного
@router.post("/", response_model=AnimalSchema)
def create_animal(animal: AnimalCreate, db: Session = Depends(get_db)):
    # 1. Pydantic схема валидирует данные
    # 2. Создаем ORM объект
    db_animal = Animal(**animal.model_dump())
    # 3. Добавляем в сессию
    db.add(db_animal)
    # 4. Сохраняем в БД
    db.commit()
    # 5. Обновляем объект (получаем ID)
    db.refresh(db_animal)
    return db_animal
```

SQLAlchemy автоматически генерирует SQL запросы для любой БД (SQLite, PostgreSQL, MySQL и т.д.)

## 🌐 API Endpoints

### Клиенты
- `GET /clients/` - список всех клиентов
- `GET /clients/{id}` - получить клиента по ID
- `POST /clients/` - создать клиента
- `DELETE /clients/{id}` - удалить клиента

### Животные
- `GET /animals/` - список всех животных
- `GET /animals/{id}` - получить животное по ID
- `POST /animals/` - создать животное
- `DELETE /animals/{id}` - удалить животное

### Врачи
- `GET /doctors/` - список всех врачей
- `GET /doctors/{id}` - получить врача по ID
- `POST /doctors/` - создать врача
- `DELETE /doctors/{id}` - удалить врача

### Лечение
- `GET /treatments/` - список всех видов лечения
- `GET /treatments/{id}` - получить лечение по ID
- `POST /treatments/` - создать лечение
- `DELETE /treatments/{id}` - удалить лечение

### Процедуры
- `GET /procedures/` - список всех процедур
- `GET /procedures/{id}` - получить процедуру по ID
- `POST /procedures/` - создать процедуру
- `DELETE /procedures/{id}` - удалить процедуру

## 🎨 Frontend

Frontend использует:
- **Bootstrap 5** - для стилизации
- **Bootstrap Icons** - иконки
- **Vanilla JavaScript** - без фреймворков

Общие файлы:
- `common.css` - общие стили для всех страниц
- `common.js` - общие функции (API запросы, валидация)

## 🔧 Настройка для разработки

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL=sqlite:///./animal_hospital.db
APP_TITLE=Veterinary Service API
APP_DESCRIPTION=API for managing veterinary clinic operations
APP_VERSION=1.0.0
```

### Режим отладки

При запуске через `python main.py` автоматически включается `reload=True` для автоматической перезагрузки при изменении кода.

## 📝 Добавление новой функциональности

### Пример: добавление нового модуля "Вакцинация"

1. **Создайте модель** (`app/models/vaccination.py`):
```python
from sqlalchemy import Column, Integer, String, Date
from app.core.database import Base

class Vaccination(Base):
    __tablename__ = "vaccination"
    vaccination_id = Column(Integer, primary_key=True)
    animal_id = Column(Integer, ForeignKey("animal.animal_id"))
    vaccine_name = Column(String(50))
    vaccination_date = Column(Date)
```

2. **Создайте схему** (`app/schemas/vaccination.py`):
```python
from pydantic import BaseModel

class VaccinationCreate(BaseModel):
    animal_id: int
    vaccine_name: str
    vaccination_date: str

class Vaccination(VaccinationCreate):
    vaccination_id: int
    class Config:
        from_attributes = True
```

3. **Создайте роутер** (`app/routers/vaccinations.py`):
```python
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.core.database import get_db
from app.models.vaccination import Vaccination
from app.schemas.vaccination import Vaccination as VaccinationSchema, VaccinationCreate

router = APIRouter(prefix="/vaccinations", tags=["Vaccinations"])

@router.post("/", response_model=VaccinationSchema)
def create_vaccination(vaccination: VaccinationCreate, db: Session = Depends(get_db)):
    db_vaccination = Vaccination(**vaccination.model_dump())
    db.add(db_vaccination)
    db.commit()
    db.refresh(db_vaccination)
    return db_vaccination
```

4. **Подключите роутер** в `main.py`:
```python
from app.routers import vaccinations
app.include_router(vaccinations.router)
```

## 🐛 Решение проблем

### Ошибка при установке зависимостей

Если возникает ошибка с компиляцией (требуется Rust):
- Обновите `pydantic` до версии >= 2.9.0 (уже в requirements.txt)
- Обновите `sqlalchemy` до версии >= 2.0.36 (уже в requirements.txt)

### Ошибка 404 для common.css/common.js

Убедитесь, что пути в HTML файлах указаны как `/static/common.css`, а не `common.css`.

### Проблемы с кэшем браузера

Нажмите `Ctrl + F5` для жесткой перезагрузки страницы.

## 📚 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Pydantic документация](https://docs.pydantic.dev/)

## 👥 Для других разработчиков

Если вы хотите использовать эту структуру в своем проекте:

1. Скопируйте структуру папок `app/`
2. Адаптируйте модели под вашу предметную область
3. Обновите роутеры и схемы
4. Настройте `config.py` под ваши нужды

**Преимущества этой архитектуры:**
- ✅ Модульность - легко добавлять новые функции
- ✅ Разделение ответственности - каждый модуль отвечает за свою часть
- ✅ Масштабируемость - легко расширять проект
- ✅ Поддерживаемость - понятная структура кода

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👤 Автор

Egorka8982

---

**Удачи в разработке! 🚀**

